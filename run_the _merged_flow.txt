Terminal 1 — Backend (Vero LLM orchestrator + itinerary planning under the hood):

uvicorn general_agent.run:app --reload --port 8001


Terminal 2 — Frontend (React card UI — user only sees Vero):

cd ui
npm run dev   # → http://localhost:3000


Optional — product site (itinero Vite app):

cd itinero
npm run dev   # → http://localhost:5173/itinero/
# VITE_VERO_API_URL=http://127.0.0.1:8001


Do NOT push changes onto manish / ShvInfotech remotes.
Work lives on branch: itinero-final-merge
